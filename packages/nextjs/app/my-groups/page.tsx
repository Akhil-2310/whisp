/**
 * Page for displaying groups that the current user has joined.
 * It will show only the groups where the connected wallet is a member.
 */
export default function MyGroups() {
  return (
    <main className="p-6">
      <h1 className="text-2xl font-bold">My Groups</h1>
      {/* TODO: Implement list of groups the user belongs to */}
    </main>
  );
}
